export type Books = {
    id: string;
    title: string;
    author: string;
    publisher: string;
    publishDate: string;
    category: string;
    status: string;
    isbn: string;
};

export type BooksForSave = {
    id: string;
    title: string;
    author: string;
    publisher: string;
    publishDate: string;
    category: string;
    status: string;
    
};

export type BookFormData = {
    title: string;
    author: string;
    publisher: string;
    publishDate: string;
    category: string;
    status: string;
    
};